// if x squared is more than 1000 return string

//P: will be integer, not negative, could be string

//R: return string based on true or false

function apple(x){
    //make x number
    //if statement
    Math.pow(Number(x),2) > 1000 ? "It's hotter than the sun!!" : "Help yourself to a honeycomb Yorkie for the glovebox."
}


//E:

apple(5)// return false string
apple(500)//return true string